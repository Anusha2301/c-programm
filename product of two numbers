#include<iostream.h>
using namespace std;
{
 int num1,num2,prd;
  cout<<"Enter a 1st number\t";
  cin>>num1;
  cout<<"Enter a 2nd number\t";
  cin>>num2;
  prd=num1*num2;
  cout<<"product  of "<<num1<<" and "<<num2<< " is"<<prd;
  return 0;
  }
